dbt_databrick:
  outputs:
    dev:
      type: databricks
      catalog: "{{ env_var('DBT_CATALOG', 'olist_dataset') }}"
      host: "{{ env_var('DBT_DATABRICKS_HOST') }}"
      http_path: "{{ env_var('DBT_DATABRICKS_HTTP_PATH') }}"
      schema: "{{ env_var('DBT_SCHEMA', 'dbt_schema') }}"
      threads: 1
      token: "{{ env_var('DBT_DATABRICKS_TOKEN') }}"
      
  target: dev